# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
env/
venv/
ENV/

# OS
.DS_Store
Thumbs.db

# ARHIAX Output & Temporal files
output_corrida/
output_referencia/dictamen_napoli.pdf.tmp
*.log

# Evitar subir PDFs y archivos comprimidos pesados
*.pdf
*.zip
*.rar
*.docx

# Excepción: las presentaciones comerciales SÍ se versionan (son entregables que
# deben poder descargarse y compartirse desde el repositorio).
!presentacion/*.pdf

# Si deseas conservar los PDFs/ZIPs de referencia específicos, descomenta:
# !motor_tma_lonja_baq_v1.0.zip

# Database and assets
*.db
api/assets/
# Caché de listas restrictivas de Titulux (ONU/OFAC/UK descargadas en vivo)
api/cache_listas/
cache_listas/
*map*.png
*map*.html
*.geojson
!api/data/*.geojson
test_geo_resultado.json

# Artefactos de pytest en Windows (permisos denegados al crear .pytest_cache)
pytest-cache-files-*/
tmp_*/
.pytest_cache/

# Páginas rasterizadas para revisión visual (derivadas del PDF de la corrida;
# se regeneran con `python scripts/golden_page_review_03i1.py`).
docs/forensics/**/paginas/

